Shared form-error data for hooks and field components.

- `FormErrors` normalizes local validator failures and API errors by field name.
- Combines touched-field messages with server responses and collects unmatched errors.
- Contains no renderer or persistence code.
