Platform services beneath UI storage and providers.

- Supplies async time helpers, active-account namespaces, and canonical storage keys.
- Native paths follow operating-system application directories, including Flatpak environments.
- Browser Web Locks coordinate operations across tabs; IndexedDB helpers live in `store-idb`.
