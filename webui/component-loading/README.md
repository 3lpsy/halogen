Provider boot component for asynchronous initialization.

- `LoadingSplash` keeps dependent content hidden until account or configuration loading completes.
- The component owns presentation only; its provider decides when to show it.
