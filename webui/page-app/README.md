Persistent application layout around routed views.

- Generic over the route enum; `routes` supplies navigation components.
- Owns scroll and row memory, confirmation menus, and player overlays.
- Keeps playback position changes from rerendering the shell.
