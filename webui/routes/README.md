Client route table, authentication guard, and application shell.

- Composes `view-*` and `page-login` crates.
- Owns typed route matching and navigation history; view crates stay independent.
- Modules: `routes`, `app`, `root_guard`.
