UI-only episode-list sorting, filtering, and selection types.

- Stable tokens represent persisted or URL-based sorting choices independently of labels.
- Custom order preserves playlist membership order; other fields use local comparisons.
- Re-exports the shared episode DTO without introducing a second domain model.
