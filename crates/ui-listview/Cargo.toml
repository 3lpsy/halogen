[package]
name = "halogen-ui-listview"
version.workspace = true
edition.workspace = true

# Episode-list view state: the sort/filter/swipe/multi-select control types plus
# the `search_sort` skeleton — pure UI data that round-trips URL queries +
# sticky storage. Named `listview` to avoid confusion with `ui-views`. A leaf.
[dependencies]
halogen-wire = { path = "../wire" }
chrono = { workspace = true }
serde = { workspace = true }
url = { workspace = true }
