# halogen-wire

WASM-safe application DTOs and validation contracts.

- Resource types use Data, StoreData, UpdateData, Params and Include conventions.
- The optional db feature adds ORM conversions.
- Request and response envelopes, pagination and includes come from wire-meta.
- Sync DTOs describe actor-scoped invalidations, tombstones and opaque cursors.
