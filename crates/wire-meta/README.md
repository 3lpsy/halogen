# halogen-wire-meta

Shared request, response and query metadata.

- Provides envelopes, includes, ordering and pagination.
- Default builds are WASM-safe; db adds ORM-facing support and http adds API errors.
- Resource-specific DTOs belong in wire.
