//! Authenticated discovery search returns partial results and per-provider errors; providers lists available toggles.
//! All provider requests run server-side and return no artwork.

pub mod providers;
pub mod search;
