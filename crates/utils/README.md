# Utils

Shared validation errors, constants, environment, paths and parsing helpers.

- Status mapping uses validation reason codes, never human error text.
- Formatting lives in `format`; `dt_human` remains re-exported for compatibility.
- Domain queries and application handlers belong in their respective crates.
